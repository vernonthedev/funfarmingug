## Fun Farming UG

Ensure that `infisical/cli` is setup globally on the machine being developed on to properly inject the secrets within the application

```bash
npm install -g @infisical/cli
```

Ensure that ur account is logged in.

```bash
infisical login
```
